name: egd-resnet
channels:
  - conda-forge
dependencies:
  - python=3.9
  - numpy=1.26.4
  - pandas
  - scipy
  - scikit-learn
  - matplotlib
  - openpyxl
  - pyyaml
  - simpleitk
  - pip
  - pip:
      - tqdm
# Install PyTorch separately after creating the environment.
# See README.md for GPU and CPU commands.
